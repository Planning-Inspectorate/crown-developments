BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[S62aCase] ADD [pressNoticeCost] DECIMAL(12,2),
[pressNoticePlaced] NVARCHAR(250),
[pressNoticeReference] NVARCHAR(250);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
