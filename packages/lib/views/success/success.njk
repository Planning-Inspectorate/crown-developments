{% from "govuk/components/panel/macro.njk" import govukPanel %}

{% macro success(title, bodyText, backLinkUrl, backLinkText) %}
    {{ govukPanel({
        titleText: title,
        html: bodyText
    }) }}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-two-thirds">
            <p class="govuk-body">
                <a href="{{ backLinkUrl }}" class="govuk-link">{{ backLinkText }}</a>
            </p>
        </div>
    </div>
{% endmacro %}
