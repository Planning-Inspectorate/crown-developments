{% extends layoutTemplate %}

{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}
{% from "govuk/components/button/macro.njk" import govukButton %}

{% set title = "Confirm redaction - " + journeyTitle + " - GOV.UK" %}

{% block pageTitle %}
    {{ title }}
{% endblock %}

{% block content %}

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            <h1 class="govuk-heading-l">
                <span class="govuk-caption-xl">{{ reference }}</span>
                Check details and redact representation
            </h1>
        </div>
    </div>

    <div class="govuk-grid-row">
        <div class="govuk-grid-column-full">
            {{ govukSummaryList({
                rows: [
                    {
                        key: {text: 'Original representation'},
                        value: {html:"<span class='pins-white-space-pre-line'>" + originalComment + "</span>"}

                    },
                    {
                        key: {text: 'Redacted representation'},
                        value: {html:"<span class='pins-white-space-pre-line'>" + commentRedacted + "</span>"},
                        actions: {
                        items: [
                            {
                                href: backLinkUrl,
                                text: "Change",
                                visuallyHiddenText: "redact"
                            }
                        ]
                    }
                    }
                ]
            }) }}
        </div>
    </div>

    <form method="POST">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-full">
                {{  govukButton({ text: 'Accept and continue' }) }}
            </div>
        </div>
    </form>

{% endblock %}
